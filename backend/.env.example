# Firebase Admin SDK — use a service account JSON or Application Default Credentials
# Option A: path to service account key file (do NOT commit the file)
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# Option B: set individual values as Cloud Run env vars
FIREBASE_PROJECT_ID=

PORT=8080
